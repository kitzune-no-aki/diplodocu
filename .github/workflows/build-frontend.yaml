name: Minimal Test
on: [workflow_dispatch, push] # Add push to trigger on commits

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test Step
        run: echo "Hello, runner is working!"
